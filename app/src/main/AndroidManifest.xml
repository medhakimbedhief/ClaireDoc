<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- INTERNET: first-time model download only (DownloadManager) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- CAMERA: ML Kit Document Scanner -->
    <uses-permission android:name="android.permission.CAMERA" />
    <!-- SET_ALARM: "Remind me" button opens system alarm picker -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />

    <application
        android:name=".ClaireDocApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ClaireDoc"
        android:largeHeap="true"
        android:configChanges="orientation|screenSize|keyboardHidden">

        <!-- GPU acceleration via OpenCL (Android 12+, Qualcomm Adreno) -->
        <uses-native-library android:name="libOpenCL.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-car.so" android:required="false" />
        <uses-native-library android:name="libOpenCL-pixel.so" android:required="false" />

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
